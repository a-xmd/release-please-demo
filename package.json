{
  "private": true,
  "name": "@axmd/release-demo",
  "version": "0.0.0",
  "type": "module",
  "main": "src/random-file.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "prettier": "^3.6.2"
  }
}
